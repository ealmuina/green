apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-celery
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery
  template:
    metadata:
      labels:
        app: celery
    spec:
      containers:
        - name: celery
          image: ealmuina/green:latest
          envFrom:
            - secretRef:
                name: green-secrets
          command: [ "celery" ]
          arguments: [ "-A", "web.tasks", "worker", "-l", "info" ]
